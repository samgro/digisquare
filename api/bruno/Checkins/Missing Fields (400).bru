meta {
  name: Missing Fields (400)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/checkins
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "placeName": "Golden Gate Park"
  }
}

docs {
  No googlePlaceId provided. Expect a 400 with zod validation details.

  Note there is no userId to omit any more: identity comes from the bearer
  token, and a userId in the body is ignored rather than rejected, so an older
  client still sending one is not broken by a 400 it cannot act on.
}
