meta {
  name: Register Invalid Email (400)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "not-an-email",
    "password": "{{testPassword}}"
  }
}

docs {
  Expect 400 with details.fieldErrors.email.
}
