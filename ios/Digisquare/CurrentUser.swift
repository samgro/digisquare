//
//  CurrentUser.swift
//  Digisquare
//

import Foundation

/// Identity of the signed-in user.
///
/// The API has no authentication yet, so this is a fixed placeholder that matches
/// the Bruno fixtures. Replace it with the real user id once the auth flow lands.
enum CurrentUser {
    static let userId = "sam"
}
